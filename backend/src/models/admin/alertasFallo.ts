import mongoose , {Schema} from 'mongoose';

const alertasFalloSchema = new Schema({
})

const alertasFalloModel = mongoose.model('alertasFalloSchema', alertasFalloSchema)

export default alertasFalloModel